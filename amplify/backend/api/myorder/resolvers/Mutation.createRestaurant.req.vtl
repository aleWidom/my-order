#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createRestaurantInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createRestaurantInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Restaurant $colStr VALUES $valStr", "SELECT * FROM Restaurant WHERE id=$ctx.args.createRestaurantInput.id"]
}