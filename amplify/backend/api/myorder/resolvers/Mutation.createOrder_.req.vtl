#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createOrder_Input.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createOrder_Input[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Order_ $colStr VALUES $valStr", "SELECT * FROM Order_ WHERE id=$ctx.args.createOrder_Input.id"]
}