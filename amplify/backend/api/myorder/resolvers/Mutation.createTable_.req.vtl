#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createTable_Input.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createTable_Input[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Table_ $colStr VALUES $valStr", "SELECT * FROM Table_ WHERE id=$ctx.args.createTable_Input.id"]
}